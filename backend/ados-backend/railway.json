{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && pip install Flask flask-cors supabase"
  },
  "deploy": {
    "startCommand": "python -c \"import os; from authservice.auth import app; app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))\"",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE"
  }
}